#!/usr/bin/env bash
set -euo pipefail

# Stable short JiHe entrypoint for formal LIBERO Stage 2 Gate training.
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
exec bash "${SCRIPT_DIR}/run_libero_stage2_gate_full_1xh100.sh" "$@"
