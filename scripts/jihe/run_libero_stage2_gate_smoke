#!/usr/bin/env bash
set -euo pipefail

# Stable short JiHe entrypoint; keep the full contract in the reviewed launcher.
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
exec bash "${SCRIPT_DIR}/run_libero_stage2_gate_smoke_1xh100.sh" "$@"
